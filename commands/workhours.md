# 导出工时记录

根据 Git 全局钩子日志，生成格式化的工时 Markdown 文件。

## 数据来源

所有 Git 提交通过全局 post-commit 钩子自动记录，日志文件位于：
```
~/.workhours/git-commit-log.txt
```

如果该文件不存在，说明尚未安装钩子，请先运行安装脚本：
```bash
curl -fsSL https://raw.githubusercontent.com/kingcwt/workhours-claude/main/install.sh | sh
```

## 参数说明

`$ARGUMENTS` 支持以下参数（可组合使用）：

### --time <时间>
- 留空 → 默认本周（本周一 00:00 至今）
- `--time 今天` / `--time today`
- `--time 本周` / `--time week`
- `--time 上周` / `--time last week`
- `--time 本月` / `--time month`
- `--time 2026-03-20` → 指定某天
- `--time 2026-03-18~2026-03-20` → 指定日期范围

### --filter <项目>
排除指定项目（多个用英文逗号分隔）：
- `--filter geo_tool`
- `--filter geo_tool,wpt`

### --c
切换为纯文本模式（不使用表格），方便粘贴复制到其他地方。

每条记录格式为：
```
`时间` `项目 / 分支` 工作描述
```

### --cp
按项目分组模式，并自动统计每个任务工时。

**时间解析规则：**
- 提交信息末尾带 `-t 2h` → 该任务花费 2 小时，展示时去掉 `-t 2h`
- 提交信息末尾带 `-t 30m` → 该任务花费 30 分钟，展示时去掉 `-t 30m`
- 支持单位：`h`（小时）、`m`（分钟），如 `-t 1.5h`、`-t 90m`
- 没有 `-t` 标记 → 自动平均分配：
  - 统计该条提交所在的那天，同一时间段（上午/下午）的总提交数 N
  - 上午时间段固定 4 小时，下午时间段固定 4 小时
  - 该条任务时长 = 4h ÷ N，结果保留 1 位小数，不足 0.5h 显示 0.5h

**输出格式：**
- 按项目名分组
- 每个项目标题右侧显示该项目总工时
- 每条记录只显示描述内容 + 工时，不显示时间和项目列
- 底部汇总所有项目总工时

### --help
显示帮助信息。

```
用法：/workhours [--time <时间>] [--filter <项目>] [--c] [--cp] [--help]

示例：
  /workhours
  /workhours --time 今天
  /workhours --time 上周
  /workhours --time 2026-03-20
  /workhours --time 2026-03-18~2026-03-20
  /workhours --time 本月 --filter geo_tool
  /workhours --filter wpt,cmc-ai
  /workhours --c
  /workhours --time 今天 --c
  /workhours --cp
  /workhours --time 本周 --cp
```

---

## 执行步骤

**第零步：解析参数**

从 `$ARGUMENTS` 中解析：
- 包含 `--help` → 输出帮助信息，结束
- 提取 `--time` 的值（默认「本周」）
- 提取 `--filter` 的值，逗号分割成列表
- 包含 `--c` → 标记为纯文本模式
- 包含 `--cp` → 标记为按项目分组工时模式（优先级高于 `--c`）

**第一步：检查日志文件**

运行以下命令确认日志文件存在：
```bash
ls -lh ~/.workhours/git-commit-log.txt
```

如果文件不存在，停止执行并提示用户先运行安装脚本（见上方数据来源）。

**第二步：确定时间范围**

根据 `--time` 的值确定 since/until（本地时区）：
- 无参数 / `本周` / `week` → 本周一 00:00 ～ 今天 23:59
- `今天` / `today` → 今天 00:00 ～ 23:59
- `上周` / `last week` → 上周一 00:00 ～ 上周日 23:59
- `本月` / `month` → 本月 1 日 00:00 ～ 今天 23:59
- `2026-03-20` → 当天 00:00 ～ 23:59
- `2026-03-18~2026-03-20` → 2026-03-18 00:00 ～ 2026-03-20 23:59

**第三步：读取并解析日志**

日志文件路径：`~/.workhours/git-commit-log.txt`

先用 grep 定位目标时间范围内的记录行号，再分段读取（避免直接读取整个大文件）：
```bash
grep -n "commit_time=<目标日期>" ~/.workhours/git-commit-log.txt
```

日志格式（每条记录以 `-----` 分隔）：
```
-----
captured_at=2026-03-19T03:31:45Z
commit_time=2026-03-19T11:31:45+08:00
project=wpt
repo=/Users/.../wpt
branch=main
hash=abc123
author=kingcwt <kingcwt@qq.com>
message<<EOF
feat: 调整编辑器UI
EOF
changed_files<<EOF
src/App.tsx
EOF
stats= 2 files changed, 10 insertions(+)
```

**第四步：过滤记录**

保留满足以下条件的提交：
1. `commit_time` 在目标时间范围内
2. `message` 不以 `checkpoint-` 开头（自动提交）
3. `message` 不以 `[agent]` 开头（AI agent 自动提交）
4. `project` 不在 `--filter` 排除列表中

**第五步：按模板生成 Markdown**

按日期分组，每天分上午（12:00 前）/ 下午（12:00 起），时间升序。

每个日期的星期几，必须通过以下 shell 命令获取，不要自己推算：
```bash
date -j -f "%Y-%m-%d" "2026-03-20" "+%u"  # 返回数字 1=周一 ... 7=周日
```
对应关系：1=周一 2=周二 3=周三 4=周四 5=周五 6=周六 7=周日

**根据参数使用不同格式：**
- 无参数（默认）→ 模板 A（表格模式）
- `--c` → 模板 B（纯文本模式）
- `--cp` → 模板 C（按项目分组 + 工时统计）

**模板 C 的工时计算步骤：**

1. 遍历所有过滤后的记录，检查每条 `message` 末尾是否有 `-t Xh` 或 `-t Xm`：
   - 有 → 解析为小时数（`30m` = 0.5h，`1.5h` = 1.5h），从展示内容中去掉 `-t ...`
   - 没有 → 标记为「待自动分配」

2. 对「待自动分配」的记录，按天+时段分组计算：
   - 同一天上午所有「待自动分配」记录共享 4h，每条 = 4 ÷ N，保留 1 位小数
   - 同一天下午所有「待自动分配」记录共享 4h，每条 = 4 ÷ N，保留 1 位小数
   - 最小单位 0.5h，不足 0.5h 按 0.5h 计

3. 按项目名分组，累加该项目所有记录的工时得到项目总工时

4. 所有项目总工时累加得到总计工时

**第六步：保存文件**

文件名规则：
- 无参数 / `--time 本周` / `--time week` → `工时记录_本周.md`
- `--time 今天` / `--time today` → `工时记录_今天.md`
- `--time 上周` / `--time last week` → `工时记录_上周.md`
- `--time 本月` / `--time month` → `工时记录_本月.md`
- `--time 2026-03-20`（指定某天）→ `工时记录_2026-03-20.md`
- `--time 2026-03-18~2026-03-20`（指定范围）→ `工时记录_2026-03-18~2026-03-20.md`

保存到 `~/Desktop/<文件名>`，告知完整路径。

---

## 输出模板

### 模板 A（默认，表格模式）

```markdown
# 工时记录

**统计周期**：2026-03-17（周一）～ 2026-03-20（周五）
**导出时间**：2026-03-20 15:30
**项目过滤**：geo_tool、wpt（仅在使用 --filter 时显示此行）

---

## 2026-03-17 周一

### 上午

| 时间 | 项目 | 工作描述 |
|------|------|----------|
| 10:27 | geo_tool | feat: 调整适应症相关逻辑 |
| 11:15 | geo_tool | fix: 调整维度1图表匹配规则 |

### 下午

| 时间 | 项目 | 工作描述 |
|------|------|----------|
| 13:46 | cmc-ai | feat: 调整sov筛选页面 |
| 18:26 | geo_tool | feat: 新增适应症支持编辑和删除 |

---

**合计提交**：XX 条（已过滤 checkpoint 自动提交 X 条）
**涉及项目**：geo_tool、cmc-ai、wpt
```

### 模板 C（--cp 按项目分组工时模式）

```markdown
# 工时记录

**统计周期**：2026-03-17（周一）～ 2026-03-20（周五）
**导出时间**：2026-03-20 15:30

---

## geo_tool `共 6.5h`

- 调整适应症相关逻辑 `1h`
- 调整受众视角提示词 `1h`
- 调整维度1图表生成关键字匹配 `1h`
- 新增适应症支持编辑和删除 `2h`
- 新增竞品其他两个名字 `1.5h`

## cmc-ai `共 3h`

- 调整分析方向相关UI `1h`
- 调整sov筛选页面和搜索条件 `1h`
- 新增分析方向提示词和调整提示词规则 `1h`

## wpt `共 4h`

- 调整编辑器UI，修复同步到微信公众号编辑器的错误提示 `1.5h`
- 右侧手机预览区域改为DOM，去除iframe `1h`
- 调整左侧编辑器浮窗选择交换逻辑 `1.5h`

---

**总计工时**：13.5h
**涉及项目**：geo_tool、cmc-ai、wpt
```

```markdown
# 工时记录

**统计周期**：2026-03-17（周一）～ 2026-03-20（周五）
**导出时间**：2026-03-20 15:30

---

## 2026-03-17 周一

### 上午

`10:27` `geo_tool / feature/311_result` feat: 调整适应症相关逻辑
`11:15` `geo_tool / feature/311_result` fix: 调整维度1图表匹配规则

### 下午

`13:46` `cmc-ai / main` feat: 调整sov筛选页面
`18:26` `geo_tool / feature/311_result` feat: 新增适应症支持编辑和删除

---

**合计提交**：XX 条（已过滤 checkpoint 自动提交 X 条）
**涉及项目**：geo_tool、cmc-ai、wpt
```

## 注意事项

- 过滤掉 `checkpoint-` 开头的自动提交
- 过滤掉 `[agent]` 开头的 AI agent 提交
- 某天只有上午或只有下午时，只显示有内容的时间段
- 没有提交记录的天不输出
- 日志文件较大时，必须用 grep 定位行号后再分段读取
